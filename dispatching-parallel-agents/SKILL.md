---
name: dispatching-parallel-agents
description: 已退役的兼容 Skill。旧流程引用此名称时，回退到当前会话内联执行。
license: MIT
metadata:
  category: compatibility
  status: retired
---

# 已退役：默认内联执行

此 Skill 已停止提供并行 Agent 调度流程，仅为兼容历史引用而保留。

默认在当前会话内联执行。除非用户明确指定其他执行方式，否则不得派遣子 Agent、创建新任务或自动切换执行模式；继续遵循当前开发模式、授权边界和验证要求。
